---
import BaseLayout from "@layouts/BaseLayout.astro";

const { title, description } = Astro.props;

const canonical = Astro.url.href;
const image = `${Astro.url.origin}/og${Astro.url.pathname.replace(/\/$/, "")}.png`;
---

<BaseLayout
  title={title}
  description={description}
  image={image}
  canonical={canonical}
>
  <slot />
</BaseLayout>
