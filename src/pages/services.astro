---
import { services } from "@data/services";
import SectionsLayout from "@layouts/SectionsLayout.astro";
import en from "@locales/en.json";

const t = en;
---

<SectionsLayout pageTitle={t.services} data={services} />
